import type { ReactNode } from "react"

type {{name}}Props = {
  children?: ReactNode,
}

const {{name}} = ({ children }: {{name}}Props) => {
  return <div>{children}</div>
}

export default {{name}}
